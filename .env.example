# Copy this file to .env and customize for your environment.
# Do NOT commit .env (secrets) to source control.

# Prisma / MySQL
DATABASE_URL="mysql://root:password@127.0.0.1:3306/linyaqms_dev"

# JWT secret for login auth (change to a strong random value)
JWT_SECRET="please_use_a_very_long_random_secret"

# Optional
NODE_ENV=development
PORT=3000
